list(APPEND IONE_INCLUDES
        ${IONE_INCLUDE_DIR}/context/context.hpp
        )
list(APPEND IONE_HEADERS
        )
list(APPEND IONE_HEADERS
        ${CMAKE_CURRENT_SOURCE_DIR}/context.cpp
        )

add_files()
